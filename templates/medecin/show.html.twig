{% extends 'base.html.twig' %}

{% block title %}Medecin{% endblock %}

{% block body %}
    <h1>Medecin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdMedecin</th>
                <td>{{ medecin.idMedecin }}</td>
            </tr>
            <tr>
                <th>Nom_prenom</th>
                <td>{{ medecin.nomPrenom }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ medecin.code }}</td>
            </tr>
            <tr>
            {% if medecin.photoFilename %}
    <img src="{{ asset('uploads/photos/' ~ medecin.photoFilename) }}" alt="Photo du médecin" style="max-width:200px;">
{% else %}
    <p>Aucune photo disponible.</p>
{% endif %}
</tr>
        </tbody>
    </table>

    <a href="{{ path('app_medecin_index') }}">back to list</a>

    <a href="{{ path('app_medecin_edit', {'id_medecin': medecin.idMedecin}) }}">edit</a>

    {{ include('medecin/_delete_form.html.twig') }}
{% endblock %}
